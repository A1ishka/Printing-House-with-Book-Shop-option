<footer>
    <div class="_footer_">
      <div class="footer_links">
        <div class="col-footer_links">
          <p>Follow us on social media:</p>
          <ul class="social-media">
            <li><a href="https://www.facebook.com/"><i class="fa-facebook"></i></a></li>
            <li><a href="https://twitter.com/"><i class="fa-twitter"></i></a></li>
            <li><a href="https://www.instagram.com/"><i class="fa-instagram"></i></a></li>
            <li><a href="https://www.youtube.com/"><i class="fa-youtube"></i></a></li>
          </ul>
        </div>
        <div class="col-footer_links">
          <p>Contact us:</p>
          <ul class="contact-info">
            <li><i class="info-phys-adress"></i>123 Main St, Anytown USA</li>
            <li><i class="info-phys-phone"></i>1-800-555-5555</li>
            <li><i class="info-phys-email"></i>info@company.com</li>
          </ul>
        </div>
      </div>
    </div>
  </footer>

  <script>
    function getJwtToken() {
        const name = 'jwtToken=';
        const decodedCookie = decodeURIComponent(document.cookie);
        const cookieArray = decodedCookie.split(';');

        //cookie-файл с именем 'jwtToken'
        for (let i = 0; i < cookieArray.length; i++) {
            let cookie = cookieArray[i];
            while (cookie.charAt(0) === ' ') { cookie = cookie.substring(1); }
            if (cookie.indexOf(name) === 0) {
            //значение cookie-файла без имени и знака '='
                return cookie.substring(name.length, cookie.length); }}
            //cookie-файл с именем 'jwtToken' не найден, возвращаем null
                return null; 
    };

    const jwtToken = getJwtToken();
    console.log(jwtToken);

    function deleteCookie(name) {
      setCookie(name, "", -1);
    }

    //deleteCookie('jwtToken');
  </script>