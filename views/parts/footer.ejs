<footer>
    <div class="_footer_">
      <div class="footer_links">
          <p>Следите за нами в социальных сетях:</p>
          <ul class="social-media"><li>
            <a href="https://www.vk.com/"><img src="../images/vk.png" width="5%" height="auto"></a>
            <a href="https://www.tumblr.com/"><img src="../images/tumbler.png" width="5%" height="auto"></a>
            <a href="https://www.youtube.com/"><img src="../images/youtube.png" width="5%" height="auto"></a>
            <a href="https://www.facebook.com/"><img src="../images/face.png" width="5%" height="auto"></a>
            </li></ul>
        </div>
        <div class="col-footer_links">
          <p>Контакты для связи:</p>
          <ul class="contact-info">
            <li><i class="info-phys-adress"></i>Минск, улица Платонова, 39</li>
            <li><i class="info-phys-phone"></i>+375-17-293-22-88</li>
            <li><i class="info-phys-email"></i>info@bookcompany.com</li>
          </ul>
        </div>
      </div>
  </footer>

  <script>
    function getJwtToken() {
        const name = 'jwtToken=';
        const decodedCookie = decodeURIComponent(document.cookie);
        const cookieArray = decodedCookie.split(';');

        //cookie-файл с именем 'jwtToken'
        for (let i = 0; i < cookieArray.length; i++) {
            let cookie = cookieArray[i];
            while (cookie.charAt(0) === ' ') { cookie = cookie.substring(1); }
            if (cookie.indexOf(name) === 0) {
            //значение cookie-файла без имени и знака '='
                return cookie.substring(name.length, cookie.length); }}
            //cookie-файл с именем 'jwtToken' не найден, возвращаем null
                return null; 
    };

    const jwtToken = getJwtToken();
    console.log(jwtToken);

    function deleteCookie(name) {
      setCookie(name, "", -1);
    }

    //deleteCookie('jwtToken');
  </script>